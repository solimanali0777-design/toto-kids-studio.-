export type Tab = 'voice' | 'dialects' | 'lab' | 'dialogue' | 'record' | 'music' | 'image' | 'video' | 'studio' | 'production' | 'growth' | 'characters' | 'bible' | 'toolbox' | 'control';
export type StyleKey = 'fun' | 'calm' | 'excited' | 'educational' | 'story' | 'silly';
export type CharacterKey = 'boy' | 'girl';
export type DialectKey = 'egyptian-white' | 'cairo-light' | 'alexandria-light' | 'delta-damietta-light' | 'saidi-light' | 'canal-light' | 'msa-simple';
export type PronunciationEntry = { id: string; term: string; sayAs: string };
export type VoiceResult = { pcmBase64: string; sampleRate: number; channels: number; bitDepth: number; voice: string; model: string };
export type VoiceQuality = { transcript?: string; score?: number; passed?: boolean; failedPhrases?: string[]; notes?: string[]; englishWords?: Array<{ word?: string; note?: string }>; model?: string };
export type AudioStats = { duration: number; peakDb: number; rmsDb: number; silencePercent: number; clippingPercent: number };
export type AudioResult = { id: string; label: string; url: string; model: string; pcmBase64: string; voice: string; stats: AudioStats; quality?: VoiceQuality; blindCode?: string };
export type Profile = { id: string; name: string; voice: string; age: number; character: CharacterKey; style: StyleKey; childStrength: number; speed: number; pitch: number; energy: number; catchphrase: string; directorNotes: string; dialectKey?: DialectKey };
export type ChannelBible = { channelName: string; dialect: string; audience: string; values: string; forbidden: string; visualStyle: string; storyRules: string; audioRules: string };
export type CapabilityData = { configured: boolean; totalModels: number; tts: string[]; transcription: string[]; music: string[]; video: string[]; image: string[]; preferred: Record<string, string | null>; resilience?: { platformTextFallback?: boolean; platformImageFallback?: boolean; browserVoiceFallback?: boolean; retryLayers?: number; dynamicModelRouting?: boolean } };
export type StageStatus = 'todo' | 'working' | 'approved';
export type ProductionStage = { id: string; label: string; status: StageStatus; reviewer: string; decidedAt: string; notes: string };
export type VideoRecord = { videoId: string; promise: string; audience: string; market: string; language: string; madeForKids: string; madeForKidsReason: string; ideaSource: string; nextPolicyReview: string };
export type RightsEntry = { id: string; asset: string; source: string; tool: string; license: string; commercial: 'review' | 'cleared' | 'blocked'; evidence: string; createdAt: string };
export type GrowthMetrics = { impressions: string; ctr: string; avgViewDuration: string; retention: string; newViewers: string; returningViewers: string; engagedViews: string; stayedToWatch: string; claims: string; rework: string };
export type MetadataPack = { titles?: string[]; description?: string; thumbnailTexts?: string[]; shortsHook?: string; parentSummary?: string; keywords?: string[]; publishNotes?: string[]; model?: string };
